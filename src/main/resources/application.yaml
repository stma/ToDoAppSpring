
spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1
#    url: ${PDB_URL}
    username: sa
    password: sa
  jpa:
    hibernate:
      ddl-auto: update
#    show-sql: true
  h2:
    console:
      enabled: true
